services:
  flask:
    build:
      context: ../../
      dockerfile: envs/shared/back/Dockerfile
      target: dev
    image: bitume-back:dev
    networks:
      - network
    volumes:
      - ${FLASK_MOUNT_POINT:-../..}:/code
    depends_on:
      - postgres
    env_file:
      - ./back/.env
    environment:
      - FLASK_APP=main.py
      - MIGRATION=1
      - SET_ENV_TO_TEST=${SET_ENV_TO_TEST:-0}
    ports:
      - "5678:5678"
      - "5001:5001"
    container_name: bitume-back

  postgres:
    build:
      context: ../shared/postgres
    image: bitume-back-postgres:dev
    ports:
      - "5432:5432"
    networks:
      - network
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    volumes:
      - bitume_back_database_dev:/var/lib/postgresql/data
    container_name: bitume-back-postgres


volumes:
  mongo_data_logs:
  bitume_back_database_dev:


networks:
  network:
