services:
  postgres:
    image: postgres:17
    container_name: bitume_postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - bitume_postgres_data:/var/lib/postgresql/data

volumes:
  bitume_postgres_data: